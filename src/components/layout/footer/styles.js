import { css } from "emotion";

export const footer = css({
    marginTop: "3rem",
    borderTop: "1px solid #ccc",
    padding: "1.5rem 0",
});

export const strong = css({
    display: "block",
});
