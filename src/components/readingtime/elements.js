import styled from "styled-components";

import { darkGray } from "../../styleConstants";

export const ReadingTimeContainer = styled.span({
    marginLeft: "1rem",
});

export const Time = styled.span({
    color: darkGray,
    paddingLeft: "0.35rem",
});
